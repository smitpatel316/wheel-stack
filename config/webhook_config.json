{
  "public_url": "https://your-domain.example/webhooks/finnhub-earnings",
  "local_url": "http://localhost:8644/webhooks/finnhub-earnings",
  "secret": "set FINNHUB_WEBHOOK_SECRET in .env instead - this placeholder is not used when the env var is set",
  "events": ["earnings"],
  "provider": "finnhub",
  "notes": "POST earnings events {symbol, date, type} to block CSPs ahead of earnings. The receiver (scripts/webhook_server.py) validates X-Finnhub-Secret against FINNHUB_WEBHOOK_SECRET from .env, logs to logs/webhook_events.jsonl, and clears the cached earnings calendar. GET /health returns {status: ok}.",
  "finnhub_api_key_env": "FINNHUB_API_KEY"
}
